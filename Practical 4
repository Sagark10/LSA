SSH is a secured shell

It provides a secure connection between two hosts over an insecure network.
This connection can also be used for terminal access,file transfers.
It can be use to support secure logins

 
Commands

-sudo apt-get -y install openssh-server

-sudo systemctl enable ssh

-sudo systemctl start ssh

-sudo systemctl status ssh
 
-sudo ssh localhost
